<% provide(:title, 'Reset password') %>
<h1>Reset password</h1>

<div class="row">
  <div class="col-md-6 col-md-offset-3">
    <%= form_for(@user, url: password_reset_path(params[:id])) do |joe| %>
      <%= render 'shared/error_messages', object: joe.object %>

      <%= hidden_field_tag :email, @user.email %>

      <%= joe.label :password %>
      <%= joe.password_field :password, class: 'form-control' %>

      <%= joe.label :password_confirmation, "Confirmation" %>
      <%= joe.password_field :password_confirmation, class: 'form-control' %>

      <%= joe.submit "Update password", class: "btn btn-primary" %>
    <% end %>
  </div>
</div>